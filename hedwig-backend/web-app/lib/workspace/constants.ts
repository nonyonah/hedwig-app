/** Cookie + localStorage key for the active workspace (must match). */
export const ACTIVE_WORKSPACE_STORAGE_KEY = 'hedwig-web-active-workspace';

export const ACTIVE_WORKSPACE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365; // 1 year
