import { Router } from 'express';

const router = Router();

// Document routes will be implemented with template generation
// Placeholder for now

router.get('/', (_req, res) => {
    res.json({ message: 'Document routes - Coming soon' });
});

export default router;
